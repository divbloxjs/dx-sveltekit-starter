export const FILE_CATEGORY = {
    PROFILE_PICTURE: "Profile Picture"
};

export const USER_ROLE = {
    ADMIN: "Admin",
    USER: "User"
};

/**
 * Used for redirects from login/register or when you land on the base '/' route
 */
export const DEFAULT_ROUTE = "/dashboard";
